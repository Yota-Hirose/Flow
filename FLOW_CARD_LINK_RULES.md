---
name: flow-card-link
description: 学習中に出会った表現を Flow(スワイプ式SRS)のカードにして、1タップで取り込める共有リンクを生成する。会話の中で「これ覚えたい」「カードにして」「Flowに登録」「取り込みリンク」と言われたとき、また英文・技術文書・資格試験の内容を読んでいて覚えるべき表現が出てきたときに使う。
---

# Flow 取り込みリンクの作り方

読んでいて詰まった表現を Flow のカード形式に整え、base64url でURLフラグメントに載せたリンクを1本出す。ユーザーはタップするだけで取り込み画面がプレビュー済みで開く。

**ホスト**: `https://flow-weld-sigma.vercel.app`
（別のデプロイ先を使っている場合はユーザーに確認して差し替える）

---

## 1. カード形式

1行1カード。

```
【日本語ヒント】 英文の一部 {{c1::答え}} 英文の残り|補足メモ / 出典: ソース名
```

- `【】` … 想起のトリガーになる日本語
- `{{c1::}}` … 思い出す対象。単語・熟語・文全体のいずれも可
- `|` 以降 … 任意。答えを見た後に出る補足
- `出典:` … 任意。カード左上に出るソースラベル

---

## 2. カードにするかの判断

**する**

- 意味は分かるが**自分では使えない**表現（受容はできるが産出できない）
- その分野で繰り返し出てくる定型（`be compensated for`, `as of`, `deprecated` など）
- 誤解していた語（多義語の別の意味、`faculty` = 教員 など）

**しない**

- 一度きりの固有名詞、専門用語そのもの
- 文脈なしでは意味を持たない断片
- すでに使える表現
- 1枚に2つ以上の学習点が入るもの → **分ける**

迷ったら作らない。カードは資産ではなく負債で、1枚増えるたびに毎日の負担が増える。

---

## 3. ヒントの書き方（ここが一番効く）

ヒントは**「思い出す努力」を起こす引き金**。ここが甘いとカードが機能しない。

**原則: 日本語で、答えの英語が一意に決まる程度に具体的に。**

| 悪い | 良い | 理由 |
|---|---|---|
| `【余裕】` | `【(金銭的に)手が届く・買える】` | 「余裕」だけでは room / margin / afford のどれか分からない |
| `【afford の意味】` | `【(金銭的に)手が届く・買える】` | 英語をヒントに置くと、想起ではなく再認になる |
| `【報酬】` | `【〜に対して報酬を受け取る】` | 品詞と使われ方まで含める |

- **ヒントに答えの英語を入れない。** 見えていたら思い出す必要がない
- 文全体を答えにするときは、ヒントに**文の和訳**を置く（`【昨日は忙しかったです】`）
- 単語を答えにするときは、ヒントに**語義**を置く（`【頻繁に】`）

---

## 4. 何を穴にするか

**思い出せなかった部分だけ**を `{{c1::}}` で囲む。

```
✅ Get the best graphics card with the most RAM you can {{c1::afford}}.
❌ Get the best graphics card {{c1::with the most RAM you can afford}}.
```

穴が長いほど正解が難しくなり、部分的に合っていても「まだ」を押すことになる。**1枚1学習点。**

冠詞・前置詞の抜けが問題なら、そこを含めて穴にする（`{{c1::to work}}`、`{{c1::compensated for}}`）。

---

## 5. 英文は原文のまま使う

**出会った文をそのまま使うのが最重要。** 例文を自分で作り直さない。

理由は2つ。文脈ごと覚えたほうが定着するのと、後から「あの技術文書で見たやつだ」と手がかりが残るため。

長すぎる場合だけ、**意味が変わらない範囲で前後を落とす**。書き換えはしない。

```
原文: TouchDesigner runs on laptops, desktops, and rackmounts. You don't need to buy
      the fastest CPU, but CPU clock speed is more important than number of cores.
✅ 短縮: CPU clock speed is more important than {{c1::number of cores}}.
❌ 作文: The clock speed matters more than the core count.
```

---

## 6. メモと出典

**メモ** … 答えを見た後に出る。「なぜ間違えたか」「他とどう違うか」を1行で。

```
|usuallyのフォーマル版。技術文書頻出
|workは無冠詞。前置詞toの抜けに注意
|※「能力・機能」の意味もある多義語
```

書くことがなければ省く。埋めるための説明は要らない。

**出典** … 後で「どこで見たか」を辿れるように。`TD UserGuide` / `TD FAQ` / `AIドリル 8/16` など短く。

---

## 7. 1文から複数のカードを作る

同じ文に学習点が2つあるときは番号を分ける。**番号ごとに別のカードになり**、対象でないクローズは答えの文字列として本文に出る。

```
【彼は毎朝走る】 He {{c1::runs}} every {{c2::morning}}.
→ 2枚
   1枚目: He ____ every morning.
   2枚目: He runs every ____ .
```

同じ番号を2回使うと1枚になり、最初の1つだけが空所になる。

---

## 8. リンクを作る

**コードを実行して符号化すること。** base64 を手で計算しない — 日本語を含むと必ず間違える。

### Node

```js
const text = [
  "【(金銭的に)手が届く・買える】 Get the best Nvidia graphics card with the most graphics RAM you can {{c1::afford}}.|can afford = 予算的に手が届く / 出典: TD FAQ",
  // …2枚目以降
].join("\n");

const b64 = Buffer.from(text, "utf8").toString("base64")
  .replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");

const link = `https://flow-weld-sigma.vercel.app/#import=${b64}`;
```

### ブラウザ / Deno

```js
const b64 = btoa(String.fromCharCode(...new TextEncoder().encode(text)))
  .replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
```

### Python

```python
import base64
b64 = base64.urlsafe_b64encode(text.encode("utf-8")).decode().rstrip("=")
link = f"https://flow-weld-sigma.vercel.app/#import={b64}"
```

### なぜフラグメント（`#`）か

**フラグメントはサーバに送信されない。** カードの中身——何を読んで何につまずいたか——がホスティングのアクセスログに残らない。クエリ（`?`）に変えてはいけない。

---

## 9. 長さの制限

| 枚数 | リンク長の目安 |
|---|---|
| 1枚 | 約350文字 |
| 10枚 | 約2,000文字 |
| 20枚 | 約4,000文字 |

**8,000文字を超えたら2本に分ける。** 一部のアプリやQRコードで扱えなくなる。

---

## 10. 出力の形

会話には**カード一覧とリンクだけ**を出す。base64の中身や符号化の説明は要らない。

````
3枚のカードにしました。

| ヒント | 答え | 出典 |
|---|---|---|
| (金銭的に)手が届く・買える | afford | TD FAQ |
| 頻繁に | frequently | TD UserGuide |
| 〜に対して報酬を受け取る | compensated for | TD UserGuide |

取り込みリンク:
https://flow-weld-sigma.vercel.app/#import=44CQ...
````

---

## 11. よくある失敗

| 失敗 | どうなるか |
|---|---|
| ヒントに英語を入れた | 想起ではなく再認になり、学習効果が消える |
| 穴が長すぎる | 部分正解が「まだ」になり、間隔が伸びない |
| 例文を作り直した | 文脈の手がかりが失われる |
| 1枚に学習点を2つ入れた | 何度も落ちる「リーチカード」になる |
| base64を手で書いた | ほぼ確実に壊れる。必ずコードを実行する |
| `?import=` にした | カードの中身がサーバのログに残る |
| 20枚以上を1本に詰めた | リンクが長すぎて開けない環境が出る |

---

## 12. 自動登録はしない

リンクを開いても**その場では登録されない**。プレビューが出て、ユーザーが「N枚を取り込む」を押して初めて入る。

これは意図した設計で、「自分で考えて作る」ことの学習効果（elaboration）を守るため。**勝手にカードを増やす提案はしない。**

---

## 付録: パーサが吸収するゆらぎ

整形に神経質にならなくてよい。以下は自動で解釈される。

- 全角の `｛｛ ｝｝ ：：`
- 行頭の箇条書き記号 `- * ・ 1. 1)`
- コードフェンス ` ``` `（中身は取り込む）
- Ankiのヒント記法 `{{c1::答え::ヒント}}`（ヒント部分は捨てられる）

読み取れない行があっても、その行だけが行番号と原因つきで表示され、成功分は登録される。
