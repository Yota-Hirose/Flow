# Flow 実装計画 — SPEC.md との差異分析と着手順

**作成**: 2026-08-16 / **更新**: 2026-08-16(**Phase 0 実装完了**)
**現在地**: Phase 0 完了 / Phase 1 は T-04 完了 → 次は **T-05(パーサ堅牢化)** または **T-25+T-06(追加フォーム・一覧)**
**対象**: SPEC.md v0.1(MVP) / 実装 commit 時点の `src/`
**位置づけ**: SPEC.md は「何を作るか」、本書は「今どこにいて、次に何をどの順で作るか」。仕様変更は SPEC.md 側に反映し、本書は完了項目を消し込む。
**関連**: `COST_ESTIMATE.md`(同期のサーバーコスト試算)

---

## 0. 結論(3行)

- SPEC §4「MVP実装済み」の**UI/演出層はほぼ仕様どおり**。差異は主にロジック層と、仕様が触れていない前提整備にある。
- **ロードマップP1(ts-fsrs移行 / リーチカード検出)は、現在のデータモデルのままでは着手できない。** カード状態に失敗回数も復習ログも無く、ストレージにマイグレーション機構も無いため。P1の前に土台工事が要る。
- **2026-08-16の決定により、Flowはサーバを持つ有料プロダクトになった**(§4)。SPEC §4.6「サーバなし・端末内完結」は廃止。これによりバックエンド基盤(Phase B)が計画に追加され、データモデルの要件が Phase 0 の時点で変わる。

---

## 1. 仕様との対応マップ

| SPEC | 項目 | 状態 | 備考 |
|---|---|---|---|
| §4.1 | 縦型1カード / max-width 420px | ✅ | `App.jsx:87` |
| §4.1 | reveal前スワイプ無効(原則1) | ✅ | `ReviewCard.jsx:49` |
| §4.1 | 閾値90px / 方向グロー・ラベル | ✅ | `ReviewCard.jsx:3,74-75` |
| §4.1 | デスクトップfallbackボタン | ✅ | `ReviewCard.jsx:181` |
| §4.1 | パーティクル16 + ハプティクス + 飛び出し | ✅ | `ReviewCard.jsx:21,41` |
| §4.1 | コンボ(3で金/5で発光/失敗リセット) | ✅ | `App.jsx:103-105` |
| §4.1 | SET_SIZE=10 / セグメントバー | ✅ | `App.jsx:10,114` — 設定で可変にする(§4-4) |
| §4.2 | 守った知識リング / ベストコンボ / 連続日数 | ✅ | `SessionComplete.jsx` |
| §4.2 | 「外したN枚は…戻ってくる」 | ✅ | `SessionComplete.jsx:34` |
| §4.3 | 期限枚数非表示 / 先取り練習の自動切替 | ✅ | `Home.jsx:10,35` |
| §4.3 | 統計チップ3種 / ストリーク | ✅ | `Home.jsx:39-43` |
| §4.4 | 貼り付け一括取り込み / 失敗行表示 | ⚠️ | 行番号は取得済みだが件数しか表示していない(`AddCards.jsx:56`)。堅牢性も不足 → **D-3 / D-15**。**さらに §4-6 の決定により、これは主導線から二次導線へ降格する** |
| §4.4 | 1枚ずつのフォーム登録 | ❌ | 存在しない。**§4-6で新たに必須と決定** → T-25 |
| §4.5 | 2択SM-2(1日→3日→×ease、下限1.3) | ✅ | `scheduler.js:14-29` |
| §4.5 | 「まだ」→ 同セッション内で再登場 | ❌ | **D-1** 未実装 |
| §4.5 | 将来のts-fsrs差し替え前提 | ❌ | **D-4** 前提が成立していない |
| §4.6 | localStorage 永続化 / サーバなし | ⚠️ | **D-5** マイグレーション機構なし。**方針転換により §4.6 自体を書き換え(§4-1,2)** |
| §4.7 | シード10枚 | ✅ | `seedCards.js` |
| §6 | デザイントークン / reduced-motion | ✅ | `styles.css` |
| §7 | 技術構成の記述 | ⚠️ | **D-11** 実際は Vite 8 + oxlint。仕様書が古い |

---

## 2. 差異一覧

深刻度: **S1**=仕様と実装が食い違う / **S2**=ロードマップの前提が欠けている / **S3**=品質・体験の穴 / **S4**=ドキュメント整合

### S1 — 仕様と実装の不一致

**D-1 「まだ」カードが同セッション内に戻ってこない** — ✅ T-04で解消
SPEC §4.5 は「due = 10分後(同セッション内で再登場し得る)」と書くが、`App.jsx:32` でセッション開始時にキュー(ID配列)を固定し `pos` を進めるだけなので、失敗カードは今のセットには二度と現れない。10分後というdueも、セッションが数分で終わる以上「同セッション内」には効かない。結果、**失敗が即時の再想起に繋がらない**——SRSで最も学習効果の高い瞬間を落としている。

**D-2 「もう1セット」で直前のカードが先頭に来る** — ✅ T-04で解消
`scheduler.js:37-43` の先取りフォールバックは全カードをdue昇順で返す。失敗カードのdueは `now+10分` で全カード中いちばん近いため、完了直後に「もう1セット」を押すと**さっき外した札から再開する**。D-1の穴を偶然埋めているが、意図した挙動ではなく、セッション境界をまたいだ分だけ間隔が不自然に潰れる。D-1と同一タスクで設計し直す。

**D-3 取り込んだカードで出典が二重表示される**
`parser.js:41` は note から `出典: X` を**抽出するだけで除去しない**。付録Aの形式 `|メモ / 出典: ソース名` を貼ると、カード左上のソースラベルと下部メモの両方に「出典: ソース名」が出る。シードカード(`seedCards.js`)は note/src が最初から分離されているためこの不整合が見えていない。

### S2 — ロードマップP1の前提欠落

**D-4 カード状態に失敗回数も復習ログも無い** — ✅ T-03で解消
`newCardState` は `{reps, interval, ease, due}` の4フィールド。SPEC §4.5 は「ts-fsrs差し替えのため4フィールドに絞った」と説明するが、**実際にはFSRSは stability / difficulty / lapses / state / last_review を要求する**ため、移行時にスキーマ変更は不可避。加えて P1「リーチカード検出」は失敗回数の累計が必須で、現状は `rate()` の失敗時に `reps=0` にするだけで**失敗した事実がどこにも残らない**。P1の2項目とも、このままでは着手不能。

**D-5 ストレージにスキーマ版管理・マイグレーションが無い** — ✅ T-02で解消
`storage.js` はキー名に `v1` を持つのみで、`loadCards()` は生JSONをそのまま返す。D-4のスキーマ変更を入れた瞬間、既存の localStorage データ(＝ドッグフーディングで貯めた実学習履歴。仕様§9の30日連続KPIの根拠そのもの)が壊れるか、`undefined` を含む状態で計算に入る。バックアップ書き出しも無いため復旧手段が無い。

**D-6 カードの編集・削除UIが無い**
追加のみで、一覧も編集も削除もできない。P1「リーチカード検出」の提案文「分解する? 削除する?」は、この導線が無いと成立しない。取り込みミス(D-15)の後始末手段も現状ゼロ。

**D-16 データモデルが同期に耐えられない** — ✅ T-02で解消
アカウント同期(§4-2)を入れる前提だと、現在のデータモデルには3つ穴がある:

- **IDがグローバル一意でない** — シードは `seed-0`〜`seed-9` の固定ID(`seedCards.js:18`)。別端末で初回起動すると**同じIDのカードが独立に生まれる**。取り込みカードも `card-${now}-${i}-${random6}` で、衝突確率はゼロではない。
- **`updatedAt` が無い** — 2端末で同じカードを編集したとき、どちらが新しいか判定できない。
- **削除を表現できない** — 配列から消すだけなので、端末Aで削除したカードが端末Bとの同期で**復活する**。

いずれも後付けが高く、先に入れるのは安い。**Phase 0 で解決する**(T-02/T-03に統合)。

### S3 — 品質・体験の穴

**D-17 英語学習専用の文言がUIにハードコードされている** — ⚠️ 一部解消(promptLabelの仕組みはPhase 0で導入済み。UIから編集する導線はT-26)
多用途化(§4-7)の実務上の障害。以下がコード中に直接埋まっている:

- `ReviewCard.jsx:147` の **「英語で言うと?」** — カード上部の常時表示ラベル。数学の公式や資格試験の用語を入れると意味が通らない
- `AddCards.jsx:25` 「多読・AIドリルで詰まった表現を…」、同 `PLACEHOLDER` の英語前提の例文
- `index.html:9` の `<title>` 「Flow — 5分の英語レビュー」

**対応方針**: 「英語で言うと?」に相当する想起トリガーの文言を**コレクションのプロパティ(`promptLabel`)にする**。コレクション作成時に1回だけ決める(英語なら「英語で言うと?」、資格試験なら「これは何?」)。設定項目を増やすのではなく、コレクションが最初から持つ属性として扱う。

**D-15 パーサがAI出力のゆらぎに弱い**
`LINE_RE`(`parser.js:11`)は `{{c1::}}` 1個・半角括弧・行頭が `【` の行しか通さない。実際のAI出力に混ざりがちな `{{c2::}}`、1行内の複数クローズ、全角 `｛｛`、箇条書きの `- ` プレフィックス、コードフェンス ``` の行はすべて失敗行になる。原則5(既存フローを無加工で接続)と成功指標「1サイクル<2分」に直撃する。さらに `AddCards.jsx:56` は失敗**件数**しか出さないので、どの行を直せばいいかユーザーに分からない(行番号は `parser.js` が返しているのに捨てている)。

**D-7 期限の到来がホーム画面に反映されない**
`App.jsx:27` の `dueCount` は deps が `[cards, view]` のみ。ホームを開いたまま放置してカードが期限を迎えても「今日の分は完了!」のままで、ボタンも「先取り練習」のまま。毎日運用する前提のアプリとしては素直に困る。

**D-8 セッションを中断できず、統計はセット完了時にしか記録されない**
session中はヘッダのホーム戻りが無効(`App.jsx:91`)で、他に離脱導線も無い。誤タップ耐性としては妥当だが、リロードで抜けた場合、カードのスケジュールだけ更新され `stats` は加算されない(`App.jsx:55`)ため、総レビュー数と実際の乖離が蓄積する。ストリークも「セット完走しないと途切れる」挙動になっており、原則「罰しない」と噛み合わない。

**D-9 テストが1本も無い**
`scheduler.js` と `parser.js` は純関数で、しかも仕様(付録Aの正規表現、間隔 1→3→×ease、ease下限1.3)が文書化されている。**ts-fsrs移行の安全網として最も費用対効果が高い**のがここ。移行前に現行挙動をテストで固定しておかないと、間隔計算が変わったのかバグなのかを後から切り分けられない。

**D-10 PWA未対応**
`index.html` に `apple-mobile-web-app-capable` はあるが、manifest も Service Worker も favicon link も無い(`public/favicon.svg` は存在するが未参照)。モバイルファースト＋毎日起動を掲げる以上P4は後ろすぎる。加えてP2「インボックス」の共有シート取り込みは **Web Share Target = manifest必須**なので、PWA化はP2の技術的前提でもある。

**D-12 キーボード操作・アクセシビリティ**
カードのreveal は `div` の onClick のみでフォーカス不可。デスクトップfallbackはボタンがあるが、Space=めくる / ↑↓=評価 のショートカットが無く、PCでの高速回しができない。

**D-13 ドラッグの取りこぼし**
`setPointerCapture` を使っていないため、指/カーソルがカード要素の外へ出ると `pointermove` が届かず追従が止まる(`pointerup` は window で拾っているので評価自体は走る)。また `dragStart.current`(ref)を描画中の transition 分岐に使っている(`ReviewCard.jsx:113`)ため、ref変更が再描画を起こさず1フレーム分ずれる。

### S4 — ドキュメント整合

**D-11 SPEC §7 と実装のズレ / ロードマップの二重管理**
SPEC §7 は「Vite 7」だが `package.json` は Vite 8 + oxlint(仕様に記載なし)。また同じロードマップが SPEC §8 と README の2箇所にあり、片方だけ更新される状態になっている。**加えて §4.6・§7・§8「意図的にやらないこと」が §4 の方針決定と矛盾するため、書き換えが必要。**

**準拠確認(問題なし)**: 原則3の非表示ルールは守られている。Home の「＋カードを追加 (N枚)」は総所持枚数であって未消化枚数ではなく、SessionComplete の「外したN枚」も §4.2 で明示的に許可された表現。

---

## 3. 実装計画

開発はAIエージェント主体のため、**所要時間ではなく依存関係の順序**で並べる。各タスクは1PR相当の粒度で、着手時にそのままエージェントへ渡せるよう受け入れ条件を付けた。上から順に実行すれば依存は満たされる。

### Phase 0 — 土台(これ以降のすべてが載る) ✅ **完了 (2026-08-16)**

> ここを飛ばして P1 に入ると、実学習データを壊すか、壊れたことに気づけない。
> **同期対応(D-16)もここに含める。** 後から足すより桁違いに安い。

**実装結果**: Vitest導入、テスト85本(全通過)、`vite build` 成功、`oxlint src` 警告ゼロ。
新規: `src/lib/id.js` / `src/lib/migrations.js` / `src/lib/reviewLog.js` / `src/lib/__tests__/*`(6ファイル)。
更新: `storage.js` / `scheduler.js` / `parser.js` / `seedCards.js` / `App.jsx` / `ReviewCard.jsx` / `AddCards.jsx`。

**T-01 テスト基盤 + 現行仕様の固定** ✅完了 ← D-9
Vitest 導入。`scheduler.rate()` の遷移(1日→3日→×ease、ease下限1.3、失敗時の10分)、`buildQueue` の優先順と先取りフォールバック、`parseCardLines` の付録A形式・失敗行番号を**現在の挙動どおりに**テスト化する。
*受け入れ*: `npm test` がCIなしで通る / scheduler・parser の分岐網羅 / 意図的にscheduler定数を1つ変えると落ちる。

**T-02 ストレージのスキーマ版管理・同期メタ・バックアップ** ✅完了 ← D-5 / D-16
`storage.js` に `SCHEMA_VERSION` と `migrate(data, from, to)` を追加。読み込み時に版を判定して逐次マイグレーション。同時に**同期の前提3点**を入れる:

- カードIDを `crypto.randomUUID()` に統一。既存カード(`seed-0` 等)はマイグレーションで採番し直す
- 全カードに `updatedAt` を持たせ、あらゆる変更で更新する
- 削除をソフト削除(`deletedAt`)に変更し、tombstone を一定期間保持する
- **コレクション(§4-7)を導入し、カードに `collectionId` を持たせる。** コレクション自体もレコード化(`id`/`name`/`promptLabel`/`updatedAt`/`deletedAt`)。既存カードとシードは「英語」コレクションへ移行する

併せて全カードのJSONエクスポート/インポート(手動バックアップ)を用意。**同期が動くまではこれが唯一の移行手段**なので品質基準は高く。
*受け入*: v1データを読んで最新版に無損失変換できるテストがある / 破損JSONでもクラッシュせずシードにフォールバック / 書き出したJSONを読み戻すと完全一致 / 2端末で独立に初回起動してもID衝突が起きない。

**T-03 カード状態の拡張と復習ログ** ✅完了 ← D-4
状態に `lapses`(累計失敗)、`lastReview`、`reviewLog[]`(`{id, ts, good, intervalBefore}`)を追加。T-02のマイグレーションで既存カードに初期値を埋める。

ログには2つの役割がある:
1. **FSRSパラメータ最適化の原資**(§4-3の解説を参照)。移行を待たず**今日から貯め始める**ことに意味がある
2. **同期の衝突解決を不要にする** — ログは追記専用なので、2端末のログを時刻順にマージするだけで済む。「カードの現在状態」はマージ後のログから再計算する設計にすれば、状態そのものを突き合わせる必要が消える。**この設計判断は T-21(同期エンジン)を大幅に簡単にするので、ここで採用しておく**

*受け入*: 既存カードが `lapses:0` / ログ空で移行される / 1レビューごとに1エントリ追加(`id` はUUID、冪等マージ可能) / カード状態をログから再構築する関数があり、既存状態と一致するテストがある / 数千枚でも容量に収まる方針が決まっている(超える場合は古いログを要約して圧縮)。

### Phase 1 — MVPの穴を塞ぐ(仕様との一致)

**T-04 セッション内再学習キュー** ✅完了 (2026-08-16) ← D-1 / D-2

**実装結果**: `src/lib/session.js` を新設(可変キュー / 進捗の導出 / コンボ算出)。テスト120本、`vite build` 成功、lint警告ゼロ。
本番ビルドに対するブラウザ通し確認も実施し、「1枚落として11タップで10枚完了・バーが again→good に戻る・リロード後もログと統計が一致」を確認済み。

**設計判断**: 進捗バーは「評価回数」ではなく**枚数**を映す。1セグメント=1枚で固定し、落としたカードは赤、取り返すとミントに戻る。再出題でバーが伸び縮みしないので、原則3の「小さな約束」が視覚的にも保たれる。カード右上の数字も `現在位置/総数` から **`確保した枚数/セットの枚数`** に変更(再出題で数字が巻き戻らない)。

**設計判断**: 先取り練習で持ち駒が全部冷却中(30分)のとき、**空のキューを返して「今日はここまで」を出す**ようにした。同じカードを続けて回させるのは分散学習の否定であり、「終わった」と言えることは原則3が守ろうとしているものそのもの。ホームとセット完了画面に、押せないボタンを出さない分岐を入れてある。

**発見したバグ**: T-02で `saveDb` が成否の真偽値を返すようにしたため、`useEffect(() => saveDb(db), [db])` の戻り値をReactがクリーンアップ関数と誤認し、2回目のレンダリングで `destroy is not a function` で落ちていた。**ユニットテストでは検出できず、ブラウザ通し確認で発覚。** → 下の「今後」参照。

キューを固定配列から可変キューに変更。「まだ」のカードは**同セット内で2〜3枚後ろに再挿入**し、正解するまでセット完了としない(SET_SIZEは「新規に触る枚数」の意味に再定義)。あわせて `buildQueue` の先取りフォールバックから、直近セッションで触ったカードを除外。SPEC §4.5 の記述もこの決定に合わせて更新する。
*受け入*: 「まだ」を押したカードが同セット内で必ず再出題される / 進捗バーが再出題で破綻しない / 完了直後の「もう1セット」に直前のカードが混ざらない / §4-4の打ち切り上限が効く。

**T-05 パーサ堅牢化と取り込みプレビュー** ← D-3 / D-15
出典を note から除去して `src` にのみ格納。`{{c2::}}` 以降・1行複数クローズ・全角括弧・箇条書きプレフィックス・コードフェンス行を許容(複数クローズは1行=複数カードに展開)。UIは取り込み前に**パース結果のプレビュー**を出し、失敗行は行番号と原文つきで表示して、その場で編集して再パースできるようにする。
*受け入*: 付録Aの正規形が従来どおり通る(T-01のテストが緑のまま) / ゆらぎパターンごとのテストがある / 失敗行が行番号つきで表示される。

**T-25 カード追加フォーム(Anki式) — 新しいデフォルト導線** ← §4-6
1枚ずつフォームで登録する画面を作り、**カード追加のデフォルトをこちらにする**。現在の一括貼り付けは「まとめて取り込む」として二次導線に降格(玄人向けとして残す)。

- 欄は4つ: **ヒント(日本語) / 英文 / メモ(任意) / 出典(任意)**
- 英文の一部を選択して「ここを隠す」で穴を開ける(Ankiの Ctrl+Shift+C 相当)
- **穴を開けなければ、英文全体が答えになる = 表・裏カードとして成立する。** モード選択やカードタイプの切り替えは出さない(原則4)
- ライブプレビュー(実際のレビュー画面と同じ見た目)を常時表示
- 追加後もフォームに留まり連続入力できる(Ankiの追加ダイアログの挙動を踏襲)

*出さないもの*: ノートタイプ、デッキ、タグ、カードタイプ選択。**Ankiから借りるのは「フォームで1枚ずつ作る」という体験だけで、Ankiの設定の海は持ち込まない**(原則4 / SPEC §4.3「セットアップ0秒」)。
*実装メモ*: 入力欄とプレビューは **T-06(編集画面)と同一コンポーネントを共有する**。追加と編集で別UIを作らない。モバイルではiOS Safariの標準テキスト選択メニューと競合するため、選択確定後に画面下部固定のボタンを出す方式にする。
*依存*: なし。ただし T-02(UUID/updatedAt)の後、T-06と同時に作るのが最も無駄がない。
*受け入*: 穴あり・穴なしの両方が登録できる / モード選択のUIが存在しない / 追加後に入力欄が空になり連続で次を打てる / プレビューが実際のレビュー表示と一致する / 一括貼り付けが従来どおり使える。

**T-06 カード一覧・編集・削除** ← D-6
一覧(検索・ソース絞り込み)、1枚編集、削除(T-02のソフト削除を使う)、複数選択削除。P1のリーチカード提案の受け皿を兼ねるので、ここで「1枚のカードを開いて操作する」画面を作っておく。
*受け入*: 一覧から編集した内容がレビューに反映される / 削除がセッション中のキューを壊さない / 数百枚でスクロールが重くならない。

**T-07 期限のライブ更新・中断導線・統計の逐次保存** ← D-7 / D-8
`dueCount` を時刻ベースで再評価(可視状態のときのみ低頻度タイマ or `visibilitychange`)。セッション中の離脱導線を追加(確認つき)。統計はカード評価ごとに加算し、ストリークは「1枚でも評価した日」で判定する(完走を条件にしない=原則「罰しない」に合わせる)。
*受け入*: 期限到来でホームの文言とボタンが自動で変わる / 中断してもレビュー済み分が統計に載る / リロードしても統計とカード状態が一致。

**T-26 コレクション(分類)** ← §4-7
T-02で入れたデータ構造をUIに出す。**新規作成・名前と `promptLabel` の編集・アクティブの切り替え**の3つだけ。

- ホームの見た目は変えない。**コレクション一覧をホームにしない**(原則3)
- 切り替えはヘッダの控えめなラベル、または設定から
- 他のコレクションの枚数・期限は表示しない
- `buildQueue` がアクティブなコレクションで絞る(T-04と整合させる)
- **ストリークと統計は全体で1本**。コレクションごとには持たない
- D-17のハードコード文言を `promptLabel` に置き換える
- **作らないもの**: 階層、コレクションごとのスケジュール設定、共有・書き出し単位としての機能

*依存*: T-02(データ構造)、T-04(キュー構築)
*受け入*: コレクションを跨いだカードが1セットに混ざらない / ホーム画面の情報量が増えていない / 他コレクションの未消化数がどこにも出ない / 「英語で言うと?」がコレクションごとに変わる / コレクションが1つだけのユーザーは存在に気づかず使える。

**T-24 設定画面** ← §4-4
設定の置き場を作る。初期の項目は **1セットの枚数(初期値10)**、再学習の打ち切り上限、エクスポート/インポート(T-02)、reduced-motion の手動オーバーライド。**将来ここにアカウント・プラン・同期状態が載る**ので、Phase B より前に骨格を作っておく。
*設計上の制約*: 設定に置いてよいのは「量とテンポの調整」まで。SPEC §8「意図的にやらないこと」のうち**バックログ枚数の表示・4択評価・受動モードは設定項目としても追加しない**(原則3・4は設定で緩めない)。この線引きを設定画面のコメントに明記する。
*受け入*: 変更が即座にセッションへ反映される / 設定は同期対象なので T-02 のスキーマに乗る / 初期値のままの人が何も考えずに済む並び順。

**T-99a SPEC/README 同期** ← D-11
Vite 8 / oxlint を §7 に反映。**§4.4 を「フォーム登録(主) + 貼り付け一括取り込み(副)」に書き換え、§4.6 を「ローカルファースト + アカウント同期」に書き換え、§7 にバックエンド構成を追加、§8「意図的にやらないこと」に §4-4 の設定方針を追記。原則4の「デッキという概念をUIに露出しない」を §4-7 の決定に合わせて書き換え(分類軸は1つ入れるが、階層・デッキ別設定・共有単位は作らない、という形に)。§1のプロダクト定義も「英語学習アプリ」から「汎用SRS」へ広げる。** ロードマップはSPEC §8を正とし、READMEからは本書へのリンクに置き換える。T-04で決めた再学習の仕様も §4.5 に反映。
*(以降、各Phase完了時に同じ同期作業を行う)*

### Phase 2 — P1: 学習の質(層Cの緩和)

**T-08 ts-fsrs 移行** ← SPEC §8 P1
`ts-fsrs` を導入し、2択(できた/まだ)を Rating へマップ(まだ=Again、できた=Good。付録Bの運用ルールどおり「意味は分かった」もGoodに寄せる)。T-02のマイグレーションで既存 `{reps,interval,ease}` からFSRSカードへ変換(初期stabilityは現行intervalから推定)。**T-01のテストを移行前後で比較できる形にしておくこと。**
*依存*: T-01, T-02, T-03
*受け入*: 既存カードが1枚も期限消失しない / 間隔がFSRSの想定範囲に収まる / スケジューラ差し替えがUI層に漏れない(`rate/isDue/buildQueue` のI/Fを維持)。

**T-09 リーチカード検出** ← SPEC §8 P1
`lapses` が閾値超過、かつ定着率が低いカードを検出し、セット完了画面で「このカード、苦しんでるね。分解する? 削除する?」を提示。分解＝T-06の編集画面へ誘導(1枚を短い2枚に割る補助)。
*依存*: T-03, T-06
*受け入*: 閾値がハードコードでなく1箇所に定義 / 提案は1セットにつき最大1枚(邪魔しない) / 却下したカードは一定期間再提案しない。

### Phase B-1 — 認証とアカウント同期(方針決定により新設)

> §4-2の決定により、**同期は無料の基本機能**として実装する。AnkiWeb相当の位置づけ。
> コストは試算済み(`COST_ESTIMATE.md`): 1,000ユーザーで月約3,700円、1万ユーザーでも約5,000円。**サーバーコストを理由に設計を削る必要はない。**
> **鉄則: ローカルファーストを崩さない。** レビュー中にネットワークを待つ実装は原則2(想起の前後を軽く保つ)に反する。ローカルが常に正で、サーバは非同期の同期先。オフラインでも全機能が動くこと。
> **T-08(FSRS移行)より後に着手する。** 先に同期を出すと、FSRSのスキーマ変更をサーバ側データにも適用する羽目になり、移行が二重に難しくなる。

**T-20 認証・アカウント基盤**
サインアップ/ログイン。**既存のローカルデータを持ったままアカウント作成でき、そのデータがそのまま初回同期で上がる**こと(ドッグフーディングで貯めた履歴を捨てさせない)。ログイン不要でも今までどおり使える状態を維持する(アカウントは同期とAIのための任意機能)。
*受け入*: 未ログインでも全ローカル機能が動く / ログイン後に既存カードが1枚も失われない / ログアウトでローカルデータを消さない。

**T-21 同期エンジン** ← §4-2
`reviewLog` は追記専用マージ(T-03の設計により衝突解決が不要)、カード本体は `updatedAt` の新しい方を採用、削除は tombstone を伝播。オフライン中の変更をキューに貯め、復帰時に送る。
*依存*: T-02, T-03, T-20
*受け入*: 2端末で交互にレビューしても履歴が失われない / オフラインでレビューした分が復帰後に反映される / 端末Aの削除が端末Bで復活しない / 同期失敗がレビュー体験を止めない。

### Phase 3 — PWA(層Aの基盤)

**T-11 PWA化** ← D-10 / P2の前提
manifest、Service Worker(アプリシェルのオフラインキャッシュ)、アイコン参照、インストール導線。**Web Share Target の宣言もここで入れる**(T-12の受け口)。
*受け入*: ホーム画面追加後にオフラインで起動しレビューできる / iOS/Androidの実機で確認 / 更新時に古いSWが residual しない。

**T-12a インボックス(AI無しの最小版)** ← SPEC §8 P2
共有シート(Share Target)から文を1タップ保存し、後でまとめて見返して**手動で**カード化する。AIの提案が無くても、「読む→カード化」のパイプ問題(層Aの本丸)はこの時点でかなり解消する。**AI待ちで止めておく理由がない。**
*依存*: T-11
*受け入*: 保存が1タップで完了しアプリを開かずに済む / 保存した文からカード編集画面へ直行できる / 未処理の件数が「雪だるま」として不安を与えない見せ方になっている(原則3)。

### Phase 4 — P3/P4: 継続の仕組みと基盤

- **T-14 復帰ボーナス**(サボり明け初回セットの報酬演出。ストリーク切れを罰しない) ← T-07後
- **T-15 休眠カード**(溜まりすぎたら自動休眠 → 余裕が出たら再浮上) ← T-08後、FSRSの負荷推定と併せて設計
- **T-16 週次レポート**「先週のあなた」 ← T-03のreviewLogが原資
- **T-17 サウンド演出**(Tone.js等) ← 独立。いつでも可
- **T-18 IndexedDB移行** ← T-02のマイグレーション機構の上で。reviewLogが膨らむため前倒しの可能性あり
- **T-19 キーボード操作・a11y・ドラッグ修正** ← D-12 / D-13。小粒・独立
- **T-27 ブラウザ通し確認の自動化** ← T-04で判明した必要性。`saveDb` の戻り値をReactがクリーンアップ関数と誤認して落ちたバグは、純関数のユニットテストでは原理的に検出できなかった。Playwright等で「初回起動 → 1セット消化 → リロード」を1本流すだけで、この種のReact配線ミスは全部捕まる。**FSRS移行(T-08)や同期(T-21)の前に入れておく価値が高い**

### Phase C — AI機能と課金(後回し ✅決定)

> §4-1の方針(サーバ側APIキー・有料提供)は維持するが、**実装は後回し**と決定(2026-08-16)。
> 同期(Phase B-1)まで完成させ、アプリとして毎日回る状態にしてから着手する。それまで収益化は発生しないため、課金・法務も同時に後ろへ倒す。

**T-23 AIプロキシ**
Claude API はサーバ側からのみ呼ぶ。**APIキーはクライアントに一切出さない。** ユーザー単位のレート制限と利用量計測をここに実装する。
*依存*: T-20
*受け入*: バンドルにキーが含まれないことを確認する仕組みがある / 1ユーザーの異常な利用がサービス全体を止めない / 利用量がユーザーごとに集計・確認できる。

**T-10 AI文脈再生成** ← SPEC §8 P1
同じ語を毎回違う例文で出題。**方式は「カードごとに例文バリエーションを事前生成して保持し、出題時にローテーション」で確定**(§4-5)。レビュー中にネットワーク待ちを挟まない。有料プランの中心的価値になる機能。
*依存*: T-23, T-06(バリエーションの編集導線)
*受け入*: オフラインでもレビューが止まらない / 生成した例文をユーザーが却下・編集できる / 生成がカード追加時の1回に限定され、レビュー回数に比例しない。

**T-12b インボックスのAI提案** ← SPEC §8 P2
T-12aで貯めた文にAIがカード候補を提案 → スワイプで採用/却下/編集。最終判断は人間に残す(原則5のelaboration保護)。
*依存*: T-12a, T-23

**T-13 Claude API直結** ← SPEC §8 P2
会話から自動でインボックスへ。
*依存*: T-12b

**T-22 課金とプランゲート**
サブスク課金。**同期は無料、AI機能が有料**(§4-2で確定)。**解約してもデータは読めてエクスポートできる**こと(データを人質にしない)。日本で販売する以上、**特定商取引法に基づく表記・利用規約・プライバシーポリシーが必要**(学習データを預かるため後者は必須)。
*依存*: T-20, T-23
*受け入*: 解約後もローカルのレビューと同期とエクスポートが動く / 決済失敗時の挙動が定義されている / 法定表示が揃っている。

---

## 4. 決定事項(2026-08-16)

### 4-1. Claude APIキー: サーバ側に置き、有料プランとして提供 / ただし実装は後回し ✅決定

BYOK(ユーザーがキーを入力)は**採用しない**。サーバレス関数を挟み、APIキーはサーバが保持する。AI機能は有料サブスクリプションの対価とする。
**ただし実装順序としては後回し**と決定(2026-08-16)。同期まで完成させてから Phase C で着手する。

**影響**: SPEC §4.6「サーバなし・端末内完結」は廃止。バックエンド・認証・課金が計画に入る。同時に、Flowは「個人のドッグフーディング用アプリ」から**課金ユーザーに対して責任を負うプロダクト**になる。データ保全・可用性・法定表示の要件が発生する(ただし課金開始まではドッグフーディングの延長として扱える)。

### 4-2. 端末間同期: やる。**無料の基本機能とする** ✅決定

アカウント単位でカード・復習ログを保存し、複数端末間で同期する(AnkiWeb相当)。**Ankiに標準である以上、有料の壁にはしない。**

**根拠**: コスト試算(`COST_ESTIMATE.md`)の結果、同期のコストは**1ユーザーあたり月0.3〜0.7円**。固定費の下限が月3,700円で、1万ユーザーに増えても月5,000円程度にしかならない。**無料ユーザーを何人抱えてもコストがほとんど増えない**構造なので、ここを課金の壁にしても守るものが小さく、AnkiWebとの比較で失うものが大きい。

**影響**: **データモデルへの影響は今すぐ発生する**(D-16)。UUID化・`updatedAt`・tombstone を Phase 0 の T-02 に前倒しで入れる。後付けは高い。着手は T-08(FSRS移行)の後(スキーマを固めてから同期を出す)。

### 4-3. FSRSの履歴: T-03を先に入れることで解決(判断不要になった) ✅決着

前回の書き方が不親切だったので、何の話か改めて書く。

FSRSは「この人はこのカードをいつ忘れるか」を予測するモデルで、その予測式には**十数個のパラメータ**が入っている。初期値は世界中の学習者の平均から作られた汎用値。Ankiに「FSRSパラメータの最適化」という機能があるが、あれは**自分の復習履歴を材料に、その数値を自分専用にチューニングする**もの。材料になるのは「いつ・どのくらいの間隔を空けて出題して・正解したか失敗したか」という1件ずつの記録で、数百件以上ないと意味のある最適化ができない。

ここで問題になるのが、**今のFlowはその記録を残していない**こと。保存しているのは「次にいつ出すか(`due`)」という結果だけで、過去に何回どう答えたかは上書きされて消えている。つまり今日までのレビュー実績は、最適化の材料としては存在しない。

なので選択肢はこうだった:

- **(a) 何もしない** → ts-fsrs移行後、汎用パラメータのまま動く。そこから記録を貯め始め、数百件たまる頃(毎日回して数ヶ月)にようやく自分専用チューニングができる
- **(b) 記録を先に貯め始める(T-03)** → 移行を待たずに今日から材料が貯まる。移行した時点で既にチューニングできる状態になっている可能性がある

**(b)を採用。** T-03はどのみち「リーチカード検出」に必須なので、追加コストはほぼゼロ。加えて §3 のT-03に書いたとおり、この記録は**同期の衝突解決も不要にする**ので、Phase Bにも効く。1つの実装が3箇所で効くため、Phase 0での優先度が高い。

**つまりこの項目はもう判断事項ではない。T-03を早く入れる、で終わり。**

### 4-4. SET_SIZE: 設定でユーザーがカスタム可能にする ✅決定

T-24(設定画面)で「1セットの枚数」を可変にする。初期値は10のまま。

**影響と注意点**: 設定項目を増やすことは原則4(判断を減らす)と部分的に衝突するが、量とテンポの調整に限れば実害は小さい(初期値のまま触らない人が大半)。**ただし線引きは守る** — SPEC §8「意図的にやらないこと」に挙げた**バックログ枚数の表示・4択評価・受動モードは、設定項目としても提供しない**。「見たい人には見せる」という譲歩が原則3を崩すのは、設定画面経由でも同じ。この方針をSPEC §8に追記する(T-99a)。

---

### 4-7. 分類(デッキ相当)を導入する — ただしAnkiのデッキとは別物にする ✅決定

英語学習以外の用途にも使えるようにするため、カードの分類軸を導入する。**名称は未定**(本書では仮に「コレクション」と呼ぶ)。

#### Ankiのデッキが担っている4つの役割のうち、必要なのは1つだけ

| デッキの役割 | Flowに必要か | 理由 |
|---|---|---|
| **① 出題範囲の分離** | **必要** | 「今日は英語だけ回したい」。多用途化の本体はこれ |
| ② デッキごとのスケジュール設定(新規上限など) | 不要 | 原則4に反する。設定の海の入口 |
| ③ 階層・フォルダ的な整理 | 不要 | Ankiの複雑さの主要因。SPEC §2.2で批判した対象そのもの |
| ④ 共有・配布の単位 | 不要 | SPEC §8で「共有デッキマーケットはやらない」と決定済み |

**①だけを取り出す。** ②③④を足した瞬間にAnkiの複雑さが再現されるので、ここは明確に線を引く。

#### 設計: 「選ぶ」のではなく「今どれを学んでいるか」が1つだけある

素直にAnk式のデッキ選択画面を作ると、**ホームに複数のデッキとそれぞれの状態が並ぶ**。これは原則3(雪だるまを見せない)の再導入になる。「5つのデッキが並んでいて、全部やらなきゃいけない気がする」——まさに避けたかった心理状態。

なので:

- カードは必ず1つのコレクションに属する
- **常に1つだけがアクティブ**で、セッションはそこからのみ組まれる
- **ホーム画面の情報量は今と変わらない。** 「今日の5分、やる?」+ 開始ボタンの1導線のまま
- 切り替えはヘッダの控えめなラベル or 設定から。**選択画面をホームにしない**
- **他のコレクションの未消化枚数・期限は一切表示しない**(原則3)
- 階層なし / コレクションごとの設定なし / 共有なし

**ストリークと統計は全体で1本にする。** コレクションごとにストリークを持つと「全部の炎を絶やせない」になり、原則3が崩れる。「今日1枚でも触った」で1本の連続日数を維持する(T-07の方針と同じ)。

#### データモデルへの影響: Phase 0 に含める

- カードに `collectionId` を持たせる → **T-02のスキーマ変更に同梱**(後付けはマイグレーション+同期で二度手間)
- コレクション自体もレコード化(`id`/`name`/`promptLabel`/`updatedAt`/`deletedAt`)し、同期対象に含める
- `buildQueue` がアクティブなコレクションで絞る(T-04と同時)
- T-25の追加フォームには「どこに入れるか」を持たせるが、**既定値は現在アクティブなコレクション**。通常は意識せず登録できる
- 既存カードとシードは移行時に「英語」コレクションへ入れる

**影響**: SPEC 原則4の「デッキという概念をUIに露出しない」という記述は書き換えが必要。ただし**趣旨(設定の海を作らない)は維持**する——今回入れるのは分類軸1つだけで、設定項目はゼロ。

### 4-6. カード登録: フォーム入力(Anki式)をデフォルトにする ✅決定

現在の一括貼り付けは**玄人向け**なので、1枚ずつフォームで登録する方式をデフォルトに変更する。貼り付け方式は二次導線として残す(原則5の「既存フローに接続」は維持)。クローズ(穴埋め)と表・裏の**両方**に対応する。

**データモデルへの影響: なし。** 表・裏カードは「`pre`/`post` が空で `answer` が文全体」という形で現在のモデルにそのまま収まる。実際、シードカードの「昨日は忙しかったです → It was really busy yesterday」(`seedCards.js:12`)が既にこの形。**つまり両方対応は新しい概念の追加ではなく、既にできることをUIで自然に扱うだけ。**

**原則4との両立**: モード選択は出さない。**英文に穴を開ければクローズ、開けなければ表・裏**、という1つのフォームで両方を吸収する。登録時にユーザーが増やす判断はゼロ。

**影響**: SPEC §4.4 は貼り付け取り込みしか記述していないため、書き換えが必要(T-99a)。実装は T-25。

### 4-5. AI文脈再生成の方式: 事前生成 + ローテーション ✅決定

レビュー毎に生成するのではなく、**カードごとに例文バリエーションを事前生成して保持し、出題時にローテーション**する。

**影響**: (1) レビュー中にネットワーク待ちが入らない(原則2を守れる)。(2) オフラインでもレビューが止まらない。(3) **生成回数がレビュー回数でなく「追加したカード枚数」に比例する**ため、1ユーザーの原価に上限が読め、価格設定が可能になる。レビュー毎生成と比べて原価が桁で下がる。

---

## 5. 残っている論点

### Phase B-1(同期)の着手前に決める

1. **バックエンドのスタック** — 試算(`COST_ESTIMATE.md` §2)では **Supabase Pro + Cloudflare Pages** を推奨。認証込みで月$25、1万ユーザーでも$34。Cloudflare一式は小規模で安いが認証を自前実装する必要があり、規模で逆転する。**Vercel Hobbyは商用利用が禁止されている**点に注意(収益化と同時にPro $20/月が必須になるため、静的配信はCloudflare Pagesへ移すのが得)。
2. **「サーバなし」を失った後の設計原則** — SPEC の5原則にサーバの話は無いが、実質的に「端末内完結」が体験の軽さを担保していた。代わりに **「ローカルファースト: サーバは同期先であって依存先ではない」** を明文化してSPEC §3に足すことを推奨。これがないと、今後の実装で少しずつネットワーク待ちが混入し、原則2が浸食される。
3. **無料アカウントのデータ保持方針** — 同期を無料にする以上、放置アカウントのデータをいつまで保持するかを決める必要がある(コスト上の問題ではなく、規約に書く必要があるという意味で)。

### 音声・画像の扱い(2026-08-16に議論、**未決**)

「Ankiのヘビーユーザーは音声・画像を多用する。対応しないとAnkiに戻られるのでは」という論点。調査した結果は以下。**決定はしていない。**

- **音声: TTS(端末内蔵の音声合成)で対応するのが有力。** Anki自身が `{{tts}}` タグでOS内蔵音声を使っており、音声ファイルを持たずに読み上げている(公式マニュアル)。Web側も SpeechSynthesis が2018年からBaseline。**サーバーコストゼロ・ストレージゼロ・オフライン動作**で、ローカルファーストの鉄則を崩さない。答え表示後にのみ再生すれば原則1(想起の聖域)も守れる。
- **これはSPEC §9との整合の問題でもある。** 最終KPIが「リスニングB1→B2」なのに、アプリに音がない。ドッグフーディング用途としてすでに穴がある。
- **画像: 対応しない方向が有力。** Ankiで画像を多用する代表格はImage Occlusion(解剖図を隠す等)で、主に医学生の用法。スワイプ1枚・5分・産出練習というFlowの体験設計と噛み合わず、**機能不足で負ける層ではなく最初からターゲットでない層**。SPEC §8「意図的にやらないこと」に追加するのが妥当。
- **判断の背景**: 自作カード方式の競合は本質的にAnkiのみで、機能量では勝てない(20年分の蓄積がある)。**メディア対応を厚くするほどAnkiの土俵に入る。** 勝てるのはAnkiが構造的に捨てられないもの(後方互換・無限の設定・4択・バックログ表示)を、最初から持たないこと。差別化を「機能の多さ」で考え始めたら赤信号。
- **判断のタイミング**: 検証手段がドッグフーディングしかない以上、「自分が音声を欲しくなった瞬間」が決め時。TTSは実装が軽く依存もないので、その時点で前倒しできる。

### Phase C(AI・課金)の着手前に決める — **今は保留でよい**

4. **AI利用量の上限設計** — 「1カードあたりバリエーションN個まで」「月あたり生成N回」など。4-5の方式決定により**原価が読める形になった**ので、実装時に数字を詰めればよい。
5. **価格** — 固定費の損益分岐は月額500円なら有料8人、1,000円なら4人(`COST_ESTIMATE.md` §4)。実際の価格はAIの原価が乗るため、T-10の実装後に確定する。
6. **法務** — 有料販売には特定商取引法に基づく表記、学習データを預かるのでプライバシーポリシー、サブスクなので利用規約(解約条件を含む)。T-22の一部。**ただし同期を無料提供する時点でプライバシーポリシーは必要になる**ので、これだけは Phase B-1 に前倒し。

---

## 6. 進め方の推奨

Phase 0 の3タスク(T-01/T-02/T-03)は**この順で連続実行**するのが安全。T-01のテストが無い状態でT-02/T-03のスキーマ変更を入れると、壊れたことに気づけない。**§4-2の決定により、T-02の内容が増えた(UUID・updatedAt・tombstone)。ここは削らないこと。** 同期を後から足すときのコストがまるごと変わる。

**全体の順序: Phase 0 → Phase 1 → Phase 2 → Phase B-1 → Phase 3 → Phase 4 → Phase C**

- **Phase 2(FSRS移行)を Phase B-1(同期)より先に。** 同期を先に出すと、FSRSのスキーマ変更をサーバ側データにも適用することになり、移行が二重に難しくなる。**スキーマを固めてから同期を出す。**
- **Phase C(AI・課金)は最後。** AIを後回しにする決定により、それに依存する課金・法務(特商法表記など)もまとめて後ろへ倒せる。同期までの間は収益化が発生しないので、ドッグフーディングの延長として身軽に進められる。
- ただし**プライバシーポリシーだけは Phase B-1 に前倒し**(無料でも他人の学習データを預かるため)。

依存さえ守れば並行可能:

- 独立して着手できる: T-05(パーサ)、T-11(PWA)、T-17(サウンド)、T-19(a11y)
- **セットで作る: T-25(追加フォーム) + T-06(一覧/編集)** — 入力欄とプレビューを共有するため、別々に作ると二度手間になる
- **T-26(コレクション)は T-04 の直後に。** どちらも `buildQueue` を触るため、別々にやると同じ関数を2回書き直すことになる
- 直列が必要: T-01 → T-02 → T-03 → T-08 → T-15
- 直列が必要(バックエンド): T-20 → T-21
- 直列が必要(AI・後回し): T-20 → T-23 → T-10 / T-12a → T-12b → T-13 / T-23 → T-22
- 決定待ち: T-20(§5-1 スタック選定後)
