# 取り込みリンク

AIとの学習会話の終わりにリンクを1本出してもらい、**タップするだけで取り込み画面がプレビュー済みで開く**仕組み。「コピーして、アプリを開いて、貼り付ける」の3手が1手になる。

## リンクの形

```
https://<Flowのホスト>/#import=<base64url>
```

`<base64url>` は、カード形式のテキスト(UTF-8)を base64 で符号化し、`+`→`-`、`/`→`_` に置き換えて末尾の `=` を落としたもの。

### なぜクエリ(`?`)ではなくフラグメント(`#`)か

**フラグメントはサーバに送信されない。** カードの中身——あなたが何を読んでいて何につまずいたか——はブラウザの外に出ず、ホスティングのアクセスログにも残らない。バックエンドを持たない今の構成では、これが最も筋がいい(SPEC 原則6: ローカルファースト)。

実測でも確認済み: リンクを開いたとき、サーバへのリクエストに `import` の値は一切含まれない。

### なぜ base64url か

日本語をパーセントエンコードすると3倍に膨らむ。base64 は約1.33倍で済む。リンクの長さは実用上の制約になるので、ここは効く。

ただし**復号は3段構え**(base64url → パーセントエンコード → 素のまま)にしてあるので、手で作られたリンクも読める。

### 長さの目安

カード10〜20枚で 1,000〜2,500文字程度。8,000文字を超えると一部のアプリやQRコードで扱いにくくなるので、それ以上は2本に分けるのが安全。

---

## Claudeに出してもらうためのプロンプト

学習会話の最後にこれを貼る。**Claudeがコードを実行できる環境**(Claude Code / Cowork / 分析ツールが使えるチャット)である必要がある——base64符号化を手計算で正確にやるのは無理なので。

````
これまでに出た表現をFlowのカードにして、取り込みリンクを1本出して。

【カード形式】1行1カード
【日本語ヒント】 英文の一部 {{c1::答え}} 英文の残り|補足メモ / 出典: ソース名

- 【】内は想起のトリガーになる日本語(語義、または文全体の和訳)
- {{c1::}} が思い出す対象。単語でも熟語でも文全体でもいい
- 1文に空所を2つ作りたいときは {{c2::}} を使う(番号ごとに別のカードになる)
- |以降は任意。答えを見た後に出る補足
- 出典: も任意

【リンクの作り方】上のテキストを text として、コードを実行して:

  const b64 = Buffer.from(text, "utf8").toString("base64")
    .replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
  const link = `https://<Flowのホスト>/#import=${b64}`;

出力は「カード一覧」と「リンク」の2つだけでいい。
````

`<Flowのホスト>` は自分のデプロイ先に置き換えること。

### ブラウザで作る場合

```js
const b64 = btoa(String.fromCharCode(...new TextEncoder().encode(text)))
  .replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
```

---

## 開いたあとの挙動

1. 取り込み画面が**貼り付けタブ**で開き、プレビューが出ている
2. 「リンクからカードを受け取った。中身を確かめて取り込む。」と表示される
3. 中身を確認して「N枚を取り込む」を押すと登録される
4. **読み込んだ時点でフラグメントは消える**ので、リロードしても二重に取り込まれない
5. アプリを開いたまま別のリンクを踏んでも拾う

読み取れない行があれば行番号と原因つきで表示され、取り込むと失敗行だけがテキストエリアに残るので、その場で直して再取り込みできる。

**登録は自動ではなく、最後の一押しを人間に残してある。** SPEC 原則5(カード作成のelaboration効果を守る)の線引きで、AIが勝手にカードを増やす設計は採らない。

---

## パーサが吸収するゆらぎ

リンク経由でも、貼り付けと同じ寛容さで読む。AIの出力にこれらが混ざっていても直す必要はない。

- 全角の `｛｛ ｝｝ ：：`
- 行頭の箇条書き記号 `- * ・ 1. 1)`
- コードフェンス ` ``` `(中身は取り込む)
- `{{c2::}}` 以降の番号 / 1行に複数のクローズ
- Ankiのヒント記法 `{{c1::答え::ヒント}}`(ヒント部分は捨てる)

---

## 実装

- `src/lib/importLink.js` — 符号化・復号・フラグメントの読み書き
- `src/App.jsx` — 起動時と `hashchange` で拾う
- `src/components/AddCards.jsx` — 受け取った状態で開く
- `src/lib/__tests__/importLink.test.js` — 往復・ゆらぎ・壊れた入力
