# Flow 仕様書

**バージョン**: 0.2
**最終更新**: 2026-08-16
**リポジトリ**: github.com/Yota-Hirose/Flow
**ステータス**: Vercelデプロイ済み。FSRS移行・設定・カード管理・1日の上限まで実装完了
**実装の現在地と残作業**: `IMPLEMENTATION_PLAN.md` を参照(タスクIDと差異IDの対応もそこにある)

---

## 1. プロダクト概要

Flowは、スワイプ操作を中心としたモバイルファーストの間隔反復学習(SRS: Spaced Repetition System)アプリである。

一言で言えば「**Ankiの学習理論はそのままに、体験の摩擦と心理的負荷を取り除いたSRS**」。第一ユーザーは開発者自身(英語学習: リーディングB2 / リスニングB1、多読+AIドリルの学習フローを毎日運用中)であり、ドッグフーディングを開発の中心に置く。

**用途は英語に限定しない(2026-08-16 決定)。** カードは「コレクション」という分類軸を1つだけ持ち、資格試験・技術用語・何にでも使える。ただし**看板は英語のまま置く**——初期のシードカードは英語で、汎用性は使い込むと分かればよい。「誰向けか分からないアプリ」になることのほうが危険だと判断した。

### コアコンセプト

> **想起の3秒は苦しいまま守り、その前後のすべてを気持ちよくする。**

SRSの学習効果は「思い出す努力」(検索練習 / retrieval practice)そのものから生まれる。この苦しさを娯楽化して薄めると学習効果が死ぬ(Duolingoが「ゲームとしては優秀、言語習得としては疑問」と評され続ける構造的原因)。したがってFlowは、想起の瞬間だけは一切妥協せず、それ以外の全接点——遷移、演出、進捗表示、完了体験、復帰導線——にドーパミン設計を集中投下する。

---

## 2. 背景: なぜ作るのか

### 2.1 出発点

開発者はAI(Claude)との英語学習フローを運用している:

1. **多読**(TouchDesigner英語ドキュメント等)で詰まった表現をマーク
2. まとめてAIに投げ、「単語か熟語か」の切り分けと意味を取得
3. AIがAnki穴埋めカード形式(`【ヒント】文 {{c1::答え}}|メモ`)で出力
4. Ankiに1枚ずつ手動コピペで登録

このフローの終端(Anki登録)が最大の摩擦点だった。同時に、Ankiというプロダクト自体への不満がコミュニティで長年蓄積されていることを調査で確認し、「不満点を構造的に潰した次世代SRS」の企画に至った。

### 2.2 Ankiユーザー不満の調査(2026-08 実施)

r/Anki(「6-year Anki retrospective」2026-08、「Why do so little people use anki」約200コメント)、Hacker News「Spaced repetition systems have gotten better」(2025-05、512コメント)等から、不満を3層に分類した。

**この3層分類が本仕様書全体の骨格である。層によって解決アプローチが異なる:**

| 層 | 内容 | 解決可能性 |
|---|---|---|
| **A. 入口の摩擦** | セットアップ難、UI/UX、カード作成の手間 | ツール設計で解決可能 |
| **B. 継続の重さ** | 復習の雪だるま化、心理的負荷、退屈さ | 設計思想の転換で解決可能 |
| **C. 手法の限界** | 孤立カードの定着不良、カード暗記化、高次理解への不向き | SRSの本質的限界。緩和は可能 |

#### 層A: 入口の摩擦(実際の声)

- 「ノートタイプ、FSRS、HTML/CSSなんて一般人は知らないし知りたくもない」
- 「オタクがオタクのために作ったプログラム。母親には絶対使えない」
- 苦労して初起動した後の感想が「これだけやって、これ?」
- HN最大の論点: 「読む→カード化」を繋ぐパイプの不在。「1クリック決済はあるのに、なぜ1クリックでAnkiに送れないのか」
- 自作カードが最も効果的と知られながら、作成が面倒で大半のユーザーは作らない

#### 層B: 継続の重さ(実際の声)

- 「溜まったカードの山を見る不安」(Anxiety from seeing cards pile up)
- 数日サボると数百枚が溜まり完全離脱する「Anki破産」
- 「2週間使ったが何も学んだ気がしない。本なら1日で進歩を感じられる」(効果実感の遅さ)
- 「退屈。最適な学習法だが、TikTokと比べたら苦行」
- 4択ボタン(Again/Hard/Good/Easy)の判断疲れ
- 6年ユーザーですら「レビューの重みで新規カード追加の余力が年々減る」

#### 層C: 手法の限界(実際の声)

- 「80回失敗したドイツ語単語がある」(孤立カード / orphan cards の定着不良)
- 単語でなく「そのカードの文全体」を暗記してしまい、実世界の新しい文脈で認識できない
- 「本1冊300枚で生涯3000分。それなら本を数回読んでブログにまとめた方がいい」
- 「Ankiは弱点を見せるのは得意だが、知識を生活に統合するのは苦手」

### 2.3 競合状況

RemNote(ノート統合+AIカード生成)、Mochi(Markdown派向け)、多数のAI×SRSスタートアップが存在する激戦区。ただし**「レビュー体験そのものの快楽化」に本気で振ったプレイヤーは不在**。開発者の放送グラフィックス/リアルタイム演出の専門性(TouchDesigner)がこのニッチでの差別化資産となる。

---

## 3. 設計原則

調査結果から導出した5原則。機能追加の判断はすべてこれに照らす。

### 原則1: 想起は聖域(層Cへの防衛線)

答えを見るまで評価操作(スワイプ)を物理的に不可能にする。受動的な「見て分かった気になる」体験を仕様レベルで排除する。

### 原則2: 快楽は前後に集中(層Bへの回答)

演出・達成感・音・ハプティクスは、想起の**後**(正解時の爆発、コンボ、セット完了)と**前**(開始の軽さ)にのみ投下する。想起中の画面は静かに保つ。

### 原則3: 雪だるまを見せない(層Bの本丸)

未消化枚数(バックログ)をユーザーに一切表示しない。「今日の5分セット」という固定の小さな約束だけを提示する。溜まった1,200枚の恐怖はAnki離脱の最大要因であり、これは数字を見せるという設計選択の帰結である。見せなければ存在しない。

### 原則4: 判断を減らす(層A・Bへの回答)

- 評価は2択(できた/まだ)。Ankiの4択が生む毎カードの判断疲れを排除。FSRS系スケジューラは2択でも精度がほぼ落ちないことが知られており、学術的にも正当化できる
- セットアップ0秒。ノートタイプ・パラメータという概念をUIに露出しない
- **分類軸は「コレクション」1つだけ持つ(2026-08-16 改訂)。** Ankiのデッキが担う4つの役割——①出題範囲の分離 ②デッキ別のスケジュール設定 ③階層・フォルダ ④共有単位——のうち、**必要なのは①だけ**。②③④を足した瞬間にAnkiの複雑さが再現される。コレクションは常に1つだけアクティブで、切り替えは設定の中に置く(ホームに一覧を出すと「全部やらなきゃ」が復活する)
- 設定に置いてよいのは「量とテンポの調整」まで。§8「意図的にやらないこと」の3項目は、**設定項目としても提供しない**

### 原則5: 既存の学習フローに接続する(層Aの本丸)

「読む→カード化」のパイプ問題に対し、AIとの学習会話で使っている出力形式をそのまま貼り付けられるインポータを提供する。カード作成のelaboration効果(自分で考えて作ることの学習効果)を守るため、AI全自動生成はせず、**人間が文脈の中で出会った表現をAIと切り分けた結果**を取り込む設計とする。

**AI出力のゆらぎはこちら側で吸収する。** 全角括弧、箇条書き記号、コードフェンス、`{{c2::}}` 以降の番号、1行に複数のクローズ——ユーザーに整形させた時点でこの原則は負けている。

### 原則6: ローカルファースト(2026-08-16 追加)

サーバは**同期先であって依存先ではない**。ローカルのデータが常に正で、サーバはその写しを預かるだけ。オフラインでもレビュー・追加・編集のすべてが動く。

この原則は原則2を守るために要る。§4.6 の方針転換でサーバを持つことになったが、「端末内完結」が担保していた体験の軽さは失われてはいけない。**レビュー中にネットワークを待つ実装は、理由を問わず却下する。**

---

## 4. 機能仕様

### 4.1 レビューセッション

**対応する不満**: 層B全般(退屈、判断疲れ、心理的負荷)

- 縦型フルスクリーン1カード表示(モバイルファースト、max-width 420px)
- フロー: ヒント表示 →(頭の中で想起)→ **タップで答え表示** → **上スワイプ「できた」/ 下スワイプ「まだ」**
- **reveal前はスワイプ無効**(原則1の実装)。カード下部に「頭の中で答えてから、タップ」の常時ガイド
- スワイプ閾値: 90px。ドラッグ量に応じてカード枠がミント(上)/レッド(下)にグロー、方向ラベルがスケールアップ
- デスクトップfallback: reveal後に「まだ」「できた ↑」ボタンを表示
- 正解時: パーティクル16個放射 + ハプティクス(navigator.vibrate)+ カードが回転しながら画面外へ飛ぶ
- コンボ: 連続正解でカウンタ表示、3連続で金色化、5連続で発光。失敗でリセット
- セッション単位: 既定10枚(設定で3〜50に変更可)。「今日の分を全部」ではなく「1セット」で必ず終わる(原則3)
- **落としたカードは同じセットに戻さない。** 答えを見た直後の再提示は想起ではなく再認のテストになり、原則1が守ろうとしている「思い出す努力」が発生しない。FSRSの再学習ステップ(数分後)に任せ、次のセットで戻す。設定で「同じセットで出し直す」に変えられる
- 進捗: セグメントバー。**1セグメント = 1枚で固定**し、再出題では伸び縮みしない(正解=ミント / 不正解=レッド / 現在=バイオレット)。残り枚数の数字は出さない
- カード右上の数字は `確保した枚数 / セットの枚数`。現在位置ではないので巻き戻らない
- セッション中の中断導線あり(二段階確認)。中断してもレビュー済みは統計に残る

### 4.2 セット完了画面

**対応する不満**: 効果実感の遅さ、罰の文化

- 「守った知識」リング(SVG円弧アニメーション)で正答数を表示
- ベストコンボ、連続日数(2日以上のみ)表示
- 不正解があった場合のコピー: 「外したN枚は、ちょうどいいタイミングで戻ってくる」— **罰ではなく再会**として言語化(原則2)
- リーチカードの提案を**1セットにつき最大1枚**だけ出す(§4.9)
- 「もう1セット」/「ホームへ」の2導線。次に回せるものが無いときは「もう1セット」を出さない(押せないボタンを作らない)

### 4.3 ホーム画面

**対応する不満**: 雪だるま不安、復帰コスト

- 表示は「今日の5分、やる?」+ セット開始ボタンのみ。期限切れ枚数は非表示(原則3)
- 期限カードゼロ時は「先取り練習」に自動で切り替わる
- 直近30分に触ったカードは先取りから外す。全部外れたら**「今日はここまで」**を出す。同じカードを続けて回させるのは分散学習の否定であり、「終わった」と言えることは原則3が守ろうとしているものそのもの
- **「それでも続ける」を控えめに置く。** 原則3が禁じているのは"アプリが要求すること"であって"ユーザーが選べること"ではない(Ankiにも Custom Study がある)。押せば上限も冷却も無視して回せる
- 期限の到来は開いたまま自動で反映される(1分刻み / 復帰時に即時)
- 統計チップ: 総レビュー / 定着率 / ベストコンボ(積み上げ系のみ。負債系の数字は置かない)
- ストリーク表示(🔥N日連続)。ただし切れても罰的な演出はしない

### 4.4 カードの追加

**対応する不満**: 層A「読む→カード化」パイプの不在

**主導線は1枚ずつのフォーム入力**(2026-08-16 変更。貼り付け形式は玄人向けなので二次導線へ降格した)。

- 欄は4つ: **ヒント(日本語) / 英文 / メモ(任意) / 出典(任意)**
- 英文の一部を選んで「選んだ部分を隠す」で穴埋めになる(Ankiの Ctrl+Shift+C 相当)
- **穴を開けなければ英文全体が答え = 表・裏カード。** モード選択もカードタイプの切り替えも出さない。ラベルが「穴埋めカード / 全文が答え」と変わるだけ(原則4)
- ライブプレビューが実際のレビュー画面と同じ見え方で出る
- 追加後もフォームに留まり、連続入力できる(Ankiの追加ダイアログの挙動)
- **出さないもの**: ノートタイプ、デッキ、タグ。Ankiから借りるのは「フォームで1枚ずつ作る」体験だけで、設定の海は持ち込まない

**二次導線: まとめて取り込む**

- 付録Aの形式を貼り付けて一括登録。AIとの学習会話の出力をそのまま流し込める(原則5)
- 取り込む前に**プレビュー**が出る。失敗行は行番号と原因つきで表示され、取り込むと**失敗行だけがテキストエリアに残る**ので、その場で直して再取り込みできる
- 吸収するゆらぎ: 全角 `｛｛ ：：`、行頭の `- * ・ 1. 1)`、コードフェンス、`{{c2::}}` 以降の番号、1行の複数クローズ(番号ごとに1枚へ展開)、Ankiのヒント記法 `{{c1::答え::ヒント}}`

**カード一覧・編集・削除**

- 検索つきの一覧から1枚ずつ編集できる。入力欄とプレビューは追加フォームと同じ部品を使う
- 削除はソフト削除。配列からは消さず、同期で「端末Aの削除が端末Bで復活する」のを防ぐ目印として残す

### 4.5 スケジューラ

**対応する不満**: 4択の判断疲れ、Ease Hell

**FSRS(ts-fsrs 公式実装)**(2026-08-16 移行。それまでは自前のSM-2系だった)。

- 評価は2択のまま。「まだ」= Again、「できた」= Good に写す。Hard / Easy は使わない
- 「まだ」を押したカードは再学習ステップ(数分後)で戻ってくる
- カードの状態は `{ due, stability, difficulty, scheduledDays, reps, lapses, fsrsState, lastReview }`。ts-fsrs との境界は `src/lib/fsrs.js` に1枚だけ置き、アプリ側は Date ではなく数値(ms)だけを扱う
- **fuzz(間隔の揺らぎ)は切ってある。** Flowは「復習ログを正としてカード状態を再計算できる」ことを同期の土台にしているため、決定性を優先した
- **短期学習ステップは有効のまま。** 切ると初回正解が3日後になる代わりに「まだ」が5日後になり、「落としたカードはすぐ戻る」という約束が壊れる

**移行で守ったこと**: 既存カードの `due` を1日も動かしていない。stability は旧 interval、difficulty は旧 ease からの推定値で、正確さより連続性を優先した。過去の復習実績は残っていない(旧実装は due しか保存していなかった)ため厳密な復元は不可能で、精度は蓄積し始めた復習ログで後から取り戻す。

**Ease Hell の解消**: 旧SM-2は Again を繰り返すと ease が下限1.3に張り付き、間隔が伸びなくなる沼があった。FSRSでは失敗を挟んでも stability が回復する。これはテストで固定してある。

### 4.6 永続化と同期

**「サーバなし・端末内完結」の方針は 2026-08-16 に撤回した。** アカウント同期と、サーバ側でAPIキーを持つAI機能を提供するため。代わりに**ローカルファースト(原則6)** を立てて、体験の軽さを守る。

- localStorage の単一キー `flow.db` にDB全体を持つ。カード・ログ・統計が別キーだと、書き込みが途中で失敗したとき互いに矛盾した状態で残る
- **スキーマのバージョン管理とマイグレーションを持つ。** 版を上げる基準は「既存データの変換が要るときだけ」。既定値を入れれば済む純粋な追加は正規化層が吸収する
- 旧キー(`flow.cards.v1`)は読み込み時に自動移行し、**復旧用に消さずに残す**
- カードIDは UUID。`updatedAt` と、削除を表すソフト削除(`deletedAt`)を持つ。同期を後から足すのは高いので先に入れた
- **復習ログは追記専用。** FSRSのパラメータ最適化の原資であり、リーチカード検出の判定材料であり、**同期の衝突解決を不要にする**(2端末のログを時刻順にマージし、カード状態はそこから再計算する)
- JSONのエクスポート/インポートを設定に用意。同期が動くまではこれが唯一の端末間移行手段
- 復習ログが膨らむ頃にIndexedDBへ移行予定

**同期(未実装)**: アカウント単位でカード・ログを保存し、複数端末で同期する。**無料の基本機能とする**——AnkiWebに標準である以上ここを有料の壁にはしない。コスト試算は `COST_ESTIMATE.md`。

### 4.7 シードデータ

初回起動時、開発者の実カード10枚(TouchDesigner UserGuide由来7枚 + AIスピーキングドリル由来3枚)を投入。空のアプリを見せない(層A: 「これだけやって、これ?」の回避)。

---

### 4.8 溜まったときの扱い

**対応する不満**: Anki破産、「溜まったカードの山を見る不安」

原則3は枚数を隠すが、**隠しただけでは「終わらなさ」として同じ恐怖が戻ってくる**。500枚溜まった状態で「今日の5分、やる?」が50回繰り返し出るなら、数字が無くても同じことになる。ここは2つの仕組みで塞ぐ。

**1日の上限**

- 設定に「1日にやるセット数」(既定3、0で無制限)。上限 = セット数 × 1セットの枚数
- 消化数は**復習ログから当日ぶんを数える**。専用カウンタを持つと中断・リロード・同期のたびに実態とずれる
- 達したらホームが「今日の分は終わり / 残りは明日また出てくる」に変わり、ボタンは「また明日」になる
- 予算は**コレクションをまたいで1本**。分類ごとに持たせると「全部やらなきゃ」が復活する
- 最後のセットは残りぶんだけに切り詰める(30枚中25枚済みなら5枚のセット)
- **壁ではない。「それでも続ける」で超えられる**

**休眠カード**

上限だけでは、消化しきれない分が翌日へ繰り越されて積み上がる。抱える量にも上限をかける。

- 期限切れが「7日分の作業量」を超えたら、超過分を自動で休眠させ、出題キューから外す
- **寝かせるのは「最も忘れているもの」から**(予定間隔に対する超過が大きい順)。直感に反するが、完全に忘れたカードをやり直すコストは新規学習とほぼ同じで今日やる価値が薄い。まだ薄っすら覚えているカードのほうが安く救える
- 余裕が出たら期限の古い順に静かに戻る
- **休眠している枚数はどこにも表示しない**(原則3)

500枚溜まった状態でも、3セットで今日が終わり、抱える量は210枚で頭打ちになる。

### 4.9 リーチカード検出

**対応する不満**: 層C「80回失敗したドイツ語単語がある」

- 失敗が閾値(既定5回)を超え、かつそのカードの正答率が60%を下回ったものを検出する
- **提案はセット完了画面に1セットあたり最大1枚**。想起中の画面は静かに保つ(原則2)
- そのセットで触ったカードの中から探す。目の前で落としたばかりのカードについて言われるほうが腑に落ちる
- 3択: **分解する / 削除する / このままでいい**
- 「このままでいい」を選んだら一定期間(既定14日)は黙る。何度も同じ提案をされるほうが苦しい

**姿勢**: Ankiの leech は既定8回失敗でタグを付けて止めるだけで、なぜ苦しいのかもどうすればいいのかも言わない。Flowは「失敗が多いのは覚えられない人が悪いのではなく、**カードの作りが悪い**ことが多い」という立場を取る。だから提案は「分解する?」から始める。1枚に詰め込みすぎた情報を2枚に割るだけで抜けることが多い。

---

## 5. 画面遷移

```
home ──セット開始──▶ session ──全カード消化──▶ complete
 ▲  ▲  ◀──ホームへ / 中断───────────────────────────┘│
 │  │                                  もう1セット─┘(→ session)
 │  └──◀ 戻る── add(追加: フォーム / 貼り付け)
 │  └──◀ 戻る── list(一覧・編集・削除)  ◀── complete の「分解する」
 └──◀ 戻る── settings(コレクション・枚数・上限・バックアップ)
```

ビュー状態は `App.jsx` の `view: "home" | "session" | "complete" | "add" | "list" | "settings"` で管理(ルータ不使用)。

---

## 6. デザイントークン

| トークン | 値 | 用途 |
|---|---|---|
| `--bg` | #0E1016 | 背景(深夜のネイビーブラック) |
| `--card` / `--card-2` | #181B26 / #14161F | カード面(グラデーション) |
| `--ink` | #F2F3F7 | 主要テキスト |
| `--dim` / `--faint` | #8B90A3 / #565B6E | 補助テキスト |
| `--mint` | #7CE7C4 | 正解・成功 |
| `--red` | #F2837A | 不正解(彩度を落とし罰感を抑制) |
| `--violet` | #8B7CFF | ブランド・アクション |
| `--gold` | #FFD583 | コンボ・称賛 |

- フォント: システムスタック(-apple-system / Hiragino Sans)。日英混在の可読性を優先
- `prefers-reduced-motion` でパーティクル無効化・アニメーション短縮

---

## 7. 技術構成

| 項目 | 選定 | 理由 |
|---|---|---|
| フレームワーク | Vite 8 + React 19 | 最小構成・高速。**Node 20.19+ / 22.12+ 必須**(rolldownのネイティブバイナリ要件。Node 20.12でstyleTextエラーの実績あり) |
| スケジューラ | ts-fsrs 5 | FSRS公式実装。Ease Hellの構造的解決(§4.5) |
| スタイリング | CSS変数 + インラインstyle | MVPの速度優先。依存ゼロ |
| 状態管理 | React useState + localStorage | ローカルファースト(原則6) |
| テスト | Vitest | データ層は純関数で書いてあり、ここが移行と同期の安全網になる |
| Lint | oxlint | `npm run lint` |
| デプロイ | Vercel(GitHub連携、静的ビルド) | push→自動デプロイ。**ただしVercel Hobbyは商用利用不可**。収益化と同時にPro($20/月)が必須になるため、静的配信はCloudflare Pagesへ移すことを検討中 |

```
src/
  App.jsx                  # 画面遷移・セッション管理・データ操作
  components/
    Home.jsx               # ホーム(雪だるま非表示の入口)
    ReviewCard.jsx         # スワイプカード(想起の聖域)
    SessionComplete.jsx    # 完了演出 + リーチ提案
    AddCards.jsx           # 追加(フォーム / 貼り付けの2タブ)
    CardEditor.jsx         # 入力フォーム(追加と編集で共有)
    CardList.jsx           # 一覧・編集・削除
    Settings.jsx           # 設定・コレクション・バックアップ
  lib/
    scheduler.js           # 出題キュー。中身はFSRS
    fsrs.js                # ts-fsrs との境界(Date↔ms、SM-2からの変換)
    session.js             # 1セットのキュー管理・進捗の導出
    dailyBudget.js         # 1日の上限
    dormancy.js            # 休眠カード
    leech.js               # リーチカード検出
    stats.js               # 統計とストリーク
    reviewLog.js           # 復習ログ(追記専用)
    storage.js             # 永続化
    migrations.js          # スキーマ版管理
    settings.js            # 設定と、設定に置いてよいものの線引き
    parser.js              # 貼り付け形式パーサ
    id.js                  # UUID
    useNow.js              # 期限のライブ更新
    __tests__/             # Vitest
  data/seedCards.js        # シード10枚
```

**バックエンド(未実装)**: 認証・同期・AIプロキシ・課金。構成は `COST_ESTIMATE.md` で **Supabase Pro + Cloudflare Pages** を推奨(認証込みで月$25、1万ユーザーでも$34)。

## 8. ロードマップ

優先度は「開発者自身の毎日の学習で困る順」。実装の詳細な進捗・タスクID・受け入れ条件は `IMPLEMENTATION_PLAN.md` が正。ここは全体像だけを持つ。

### 完了 (2026-08-16)

- [x] **土台**: テスト基盤、スキーマ版管理とマイグレーション、UUID化・`updatedAt`・ソフト削除(同期の前提)、復習ログ
- [x] **ts-fsrs移行**: Ease Hell根絶(§4.5)
- [x] **リーチカード検出**: 「このカード、苦しんでるね。分解する?」(§4.9)
- [x] **カード管理**: フォーム追加を主導線に、一覧・編集・削除
- [x] **パーサ堅牢化**: AI出力のゆらぎを吸収、取り込みプレビュー
- [x] **コレクション**: 英語以外にも使える分類軸(§4.4 / 原則4)
- [x] **設定**: 1セットの枚数、再出題、1日の上限、バックアップ
- [x] **1日の上限と休眠カード**: 溜まっても「今日の終わり」が来る(§4.8)

### P1: 学習の質(層Cの緩和)

- [ ] **AI文脈再生成**: 同じ語を毎回違う例文で出題。「カードを暗記してしまう」問題への直接的解毒剤。**方式は「カードごとに例文を事前生成してローテーション」で確定**——レビュー中にネットワークを待たない(原則6)うえ、生成回数がレビュー回数でなく追加枚数に比例するので原価が読める

### P2: 入口の摩擦ゼロ化(層Aの完成)

- [ ] **PWA化**: manifest、Service Worker、オフライン。**Web Share Target の前提**でもある
- [ ] **インボックス(AI無しの最小版)**: 共有シートから1タップ保存 → 後でまとめて手動でカード化。AI待ちで止めておく理由がない
- [ ] **インボックスのAI提案**: 貯めた文にAIがカード候補を出し、スワイプで採用/却下/編集。最終判断は人間に残す(原則5)
- [ ] **Claude API直結**: 会話から自動でインボックスへ

### P3: 継続の仕組み(層Bの完成)

- [ ] **復帰ボーナス**: サボり明けの初回セットに報酬演出。ストリーク切れを罰しない
- [ ] **週次レポート**: 「先週のあなた」— 定着した語、救出したリーチカードを演出付きで
- [ ] **サウンド演出**: 正解音・コンボ音(Tone.js等)。開発者のリアルタイム演出スキルの投下先
- [ ] **音声(TTS)**: 端末内蔵の音声合成で読み上げる。**音声ファイルは持たない**——Anki自身も `{{tts}}` でOS音声を使っており、原価ゼロ・オフライン動作で原則6を崩さない。§9の最終KPIがリスニングである以上、優先度は見た目より高い

### P4: 基盤

- [ ] **認証とアカウント同期**: 無料の基本機能とする(§4.6)
- [ ] **AIプロキシと課金**: APIキーはサーバ側。AI機能が有料の対価
- [ ] **ブラウザ通し確認の自動化**: 純関数のテストでは検出できないReact配線ミスを捕まえる
- [ ] IndexedDB移行(復習ログが膨らむ頃)
- [ ] 端末間同期の衝突解決の実地検証

### 意図的にやらないこと

**この3つは設定項目としても提供しない。** 「見たい人もいる」への譲歩は、設定画面経由でも同じように原則を壊す。

- **受動モード**(答えを自動表示して流し見)— 原則1に反する
- **バックログ枚数の表示オプション**— 原則3に反する
- **4択評価への拡張**— 原則4に反する

そのほか:

- **共有デッキマーケット**(当面)— 調査で確認した「他人のデッキは定着しない」問題。自分の文脈で出会った表現のカード化が本質価値
- **画像(Image Occlusion等)**— Ankiで画像を多用する代表格は医学生の解剖図で、スワイプ1枚・5分・産出練習というFlowの体験設計と噛み合わない。**機能不足で負ける層ではなく、最初からターゲットでない層**。自作カード方式の競合は本質的にAnkiのみで機能量では勝てない以上、メディアを厚くするほど不利になる
- **デッキの階層・デッキ別設定・共有単位**— 原則4(コレクションは分類軸1つだけ)

---

## 9. 成功指標(ドッグフーディング期)

- 開発者自身が**30日連続**でFlowのみでレビューを完結できるか(Ankiへ戻らないか)
- 「多読→AI→Flow取り込み」の1サイクル所要時間 < 2分
- 12月のEF SET再受験でリスニングB1→B2到達(アプリ単体の指標ではないが、学習フロー全体の最終KPI)

---

## 付録A: 取り込み形式の完全定義

```
【<日本語ヒント>】 <前文>{{c1::<答え>}}<後文>|<補足メモ> / 出典: <ソース名>
```

- `【】`内: 想起のトリガーとなる日本語(語義または文全体の和訳)
- `{{c1::}}`: 産出対象。単語・熟語・文全体のいずれも可
- `|`以降: 任意。表示は答え表示後
- `出典:`: 任意。カード左上のソースラベルへ移し、**メモ側からは取り除く**(二重表示しない)

**許容するゆらぎ**(§4.4 / 原則5): 全角 `｛｛ ｝｝ ：：` / 行頭の `- * ・ 1. 1)` / コードフェンス / `{{c2::}}` 以降の番号 / 1行に複数のクローズ(番号ごとに1枚へ展開し、対象でないクローズは答えの文字列として本文に出す) / Ankiのヒント記法 `{{c1::答え::ヒント}}`(ヒントは捨てる)

### レビュー時の評価基準(運用ルール)

- ヒントを見て英語が口から出た → **できた**
- 出なかったが答えを見て文の意味は分かっていた → 本来Hard相当だが、2択では**できた**(意味理解を失敗扱いにしない)
- 意味自体が分からなかった → **まだ**

FSRSへは Good / Again の2値で渡す。Hard / Easy は使わない(原則4)。

## 付録B: 調査ソース

- r/Anki「6-year Anki retrospective! Thoughts on the long-term strengths and weaknesses of Anki」(2026-08)
- r/Anki「Why do so little people use anki despite how effective it obviously is」(約200コメント)
- Hacker News「Spaced repetition systems have gotten better」(2025-05、512コメント)
- 関連知見: FSRS/SM-2のEase Hell問題、検索練習・分散学習のメタ分析群、タスク反復研究
